# Changelog

## 0.1.0 — Initial Release

- Chat participant `@casecraft` with `/generate`, `/query`, `/ingest`, `/config` commands
- Copilot model integration via VS Code Language Model API
- Python bridge for CaseCraft core engine
- RAG-powered freeform QA chat
- Knowledge base ingestion from docs, URLs, and sitemaps
